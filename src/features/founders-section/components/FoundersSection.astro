---
import { ArrowUpRight } from "@lucide/astro";
import Container from "@/components/shared/Container.astro";
import SectionIntro from "@/components/shared/SectionIntro.astro";
import GhostButton from "@/components/shared/GhostButton.astro";
import PersonAvatar from "@/components/shared/PersonAvatar.astro";
import FadeIn from "@/components/animations/FadeIn.astro";
import { FOUNDERS, TEAM_HEADCOUNT, TEAM_MEMBERS } from "@/constants/people";
import { FOUNDERS_CONTENT, TEAM_STRIP } from "../constants";
import FounderCard from "./FounderCard.astro";

/** Port of features/founders-section/components/founders-section.tsx. */

const STACK = 5;
const stack = TEAM_MEMBERS.slice(0, STACK);
const remaining = TEAM_HEADCOUNT - stack.length;
---

<section
  id="founders"
  class="relative overflow-hidden bg-[#0f0f12] py-12 text-white sm:py-24 lg:py-28"
  aria-labelledby="founders-title"
>
  <Container class="relative z-10">
    <SectionIntro
      tone="dark"
      align="center"
      label={FOUNDERS_CONTENT.label}
      title={FOUNDERS_CONTENT.title}
      description={FOUNDERS_CONTENT.description}
      titleId="founders-title"
      descriptionClass="max-w-160"
    />

    <div class="mt-8 grid gap-4 sm:mt-16 sm:gap-5 lg:grid-cols-2 lg:gap-6">
      {
        FOUNDERS.map((founder, i) => (
          <FadeIn delay={0.08 * (i + 1)}>
            <FounderCard founder={founder} index={i} />
          </FadeIn>
        ))
      }
    </div>

    <FadeIn delay={0.24}>
      <div
        class="relative mt-4 rounded-lg bg-linear-to-b from-white/[0.16] via-white/[0.06] to-white/[0.03] p-px sm:mt-5 lg:mt-6"
      >
        <div
          class="relative flex h-full flex-col overflow-hidden rounded-[15px] bg-[#18181b] gap-5 p-5 sm:gap-7 sm:p-8 lg:flex-row lg:items-center lg:justify-between lg:gap-12"
        >
          <span
            aria-hidden="true"
            class="pointer-events-none absolute inset-x-10 top-0 h-px bg-linear-to-r from-transparent via-white/28 to-transparent"
          ></span>

          <div class="relative min-w-0">
            <p class="text-eyebrow text-white/72">
              {TEAM_STRIP.label}
            </p>
            <h3 class="mt-2.5 text-[19px] font-bold leading-tight tracking-[-0.02em] text-white sm:text-[23px]">
              {TEAM_STRIP.title}
            </h3>
            <p class="mt-2.5 max-w-[58ch] text-[14px] leading-[1.6] text-white/82 sm:text-[15.5px]">
              {TEAM_STRIP.description}
            </p>
          </div>

          <div class="relative flex shrink-0 flex-col gap-5 sm:flex-row sm:items-center sm:gap-6 lg:flex-col lg:items-end lg:gap-4">
            <ul class="flex items-center -space-x-1.5 sm:-space-x-2">
              {
                stack.map((member) => (
                  <li title={`${member.name} — ${member.role}`}>
                    <PersonAvatar
                      name={member.name}
                      image={member.image}
                      size={48}
                      tone="muted"
                      class="ring-2 ring-[#17171a]"
                    />
                  </li>
                ))
              }
              <li
                aria-hidden="true"
                class="flex h-12 w-12 items-center justify-center rounded-full bg-white/9 text-[12.5px] font-bold text-white/88 ring-2 ring-[#17171a]"
              >
                +{remaining}
              </li>
            </ul>

            <GhostButton
              tone="light"
              href={TEAM_STRIP.cta.href}
              class="w-full shrink-0 sm:w-auto"
            >
              {TEAM_STRIP.cta.label}
              <ArrowUpRight class="h-4 w-4" aria-hidden="true" />
            </GhostButton>
          </div>
        </div>
      </div>
    </FadeIn>
  </Container>
</section>
