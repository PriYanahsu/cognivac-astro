---
import { cn } from "@/lib/utils";

/** Port of the SlideUpItem export in components/animations/slide-up.tsx. */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div data-reveal-item class={cn(className)}>
  <slot />
</div>
